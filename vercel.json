{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm set registry https://registry.npmjs.org/ && pnpm install --no-frozen-lockfile"
      }
    }
  ]
}
